# TASK-040.3: Add tests for GET /queues/:queueName - error handling

**Section**: 5. HTTP/API Black-box Tests (App)
**Subsection**: 5.3.3
**Task ID**: TASK-040.3

## Description

Add a test case for the `GET /queues/:queueName` endpoint to verify it correctly handles errors when `getQueueInfo` throws. The test should verify the endpoint returns 500 with error JSON.

**Reference Implementation**: 
- `virtual-assistant/cursor-agents/src/app.ts` - `GET /queues/:queueName` endpoint
- `virtual-assistant/cursor-agents/tests/app.test.ts` - existing test structure

## Current State

The `GET /queues/:queueName` endpoint:
- Returns 500 when getQueueInfo throws
- Returns error JSON with error message

The test file `tests/app.test.ts` exists with previous tests including TASK-040.1 and TASK-040.2.

**Important**: This task should be executed after TASK-040.2, completing TASK-040.

## Checklist

### Preparation and Setup

- [ ] Review `GET /queues/:queueName` endpoint error handling
- [ ] Understand error response format
- [ ] Review test setup from previous subtasks

### Implementation Steps

- [ ] Step 1: Write test for error path
  - [ ] Create test: `it('should return 500 when getQueueInfo throws', async () => { ... })`
    - [ ] Arrange: 
      - [ ] Mock `getQueueInfo` to throw error
    - [ ] Act: GET `/queues/q1`
    - [ ] Assert: 
      - [ ] Response status is 500
      - [ ] Response body contains error JSON

### Specific Requirements

- [ ] Requirement 1: Test must verify error handling
  - Acceptance: Returns 500 with error JSON
- [ ] Requirement 2: Test must verify error message
  - Acceptance: Error message is included in response

### Error Handling and Edge Cases

- [ ] Test with various error types
- [ ] Verify error messages are descriptive

### Testing

- [ ] Run `npm run test:http` to verify test executes
- [ ] Verify test passes consistently
- [ ] Verify existing tests still pass
- [ ] Run full test suite to ensure no regressions
- [ ] **DO NOT manually test by running the server** - use automated tests only

### Documentation

- [ ] Add comments explaining error handling

### Verification

- [ ] Verify test passes consistently
- [ ] Verify error handling works
- [ ] Confirm tests follow project conventions

## Notes

- This task is part of **Phase 5 – HTTP/API Black-box Tests (App)**
- **Execution Timing**: Should be executed after TASK-040.2 (completes TASK-040)
- **Dependencies**: 
  - TASK-040.2: Add tests for GET /queues/:queueName - missing queue
- **Important Considerations**: 
  - This is the third and final subtask for TASK-040
- **Task Independence**: Can be completed independently after TASK-040.2
- **Current State**: Test file exists with previous tests

## Related Tasks

- Parent: TASK-040 (Add tests for /queues/:queueName)
- Previous: TASK-040.2 (Add tests for GET /queues/:queueName - missing queue)
- Next: TASK-041 (Add tests for /queues/:queueName DELETE)
- Dependencies:
  - TASK-040.2: Add tests for GET /queues/:queueName - missing queue
- Related:
  - TASK-040.1, TASK-040.2: Other subtasks

## Definition of Done

### Task Type: TESTING TASK

**Description**: This task involves adding a test case for `GET /queues/:queueName` error handling.

**Definition of Done**: "A test case is written that verifies `GET /queues/:queueName` correctly returns 500 with error JSON when getQueueInfo throws. The test passes consistently. All changes have been committed to git and pushed to origin."

### User Stories

1. **As a developer**, I want to verify error handling works so that errors are handled gracefully.
   - **Acceptance Criteria**:
     - Test verifies errors return 500
     - Test verifies error JSON is returned

### Automated Tests

The test case should be:

```typescript
describe('GET /queues/:queueName', () => {
  it('should return 500 when getQueueInfo throws', async () => {
    // Arrange: getQueueInfo throws
    mockQueueManager.getQueueInfo.mockImplementation(() => {
      throw new Error('Database error');
    });
    
    // Act
    const response = await request(app.getApp())
      .get('/queues/q1')
      .expect(500);
    
    // Assert
    expect(response.body).toHaveProperty('error');
  });
});
```

- [ ] Test case written and passes
- [ ] Test verifies error handling

### Git Commit and Push Requirements

- [ ] Test case added to `tests/app.test.ts`
- [ ] Commit message follows project conventions (e.g., "test: add GET /queues/:queueName error handling test")
- [ ] Changes are pushed to origin
- [ ] Commit includes only this test case

### Final Checklist

- [ ] Test case written and passes
- [ ] Test verifies error handling
- [ ] All changes committed to git
- [ ] Code pushed to origin
- [ ] Task marked as complete
- [ ] **TASK-040 (all subtasks) is complete**

