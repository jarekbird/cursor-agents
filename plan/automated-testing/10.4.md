# TASK-010.4: Test getNextReadyTask when query fails (returns null, logs error)

**Section**: 1. DatabaseService Unit Tests
**Subsection**: 1.7.4
**Task ID**: TASK-010.4

## Description

Add a test case for the `getNextReadyTask` method to verify it returns `null` and logs an error when the database query fails. This verifies error handling behavior.

**Reference Implementation**: 
- `virtual-assistant/cursor-agents/src/services/database-service.ts` (lines 111-135) - `getNextReadyTask()` method

## Current State

The `getNextReadyTask` method:
- Queries `tasks` table for tasks with `status IN (0, 4)`
- Returns `null` on error and logs the error

The test file `tests/services/database-service.test.ts` exists with previous tests including TASK-010.1, TASK-010.2, and TASK-010.3.

**Important**: This task should be executed after TASK-010.3, completing TASK-010.

## Checklist

### Preparation and Setup

- [ ] Review `getNextReadyTask` error handling implementation
- [ ] Understand how to simulate query failures
- [ ] Review logger mocking patterns
- [ ] Review test setup from previous subtasks

### Implementation Steps

- [ ] Step 1: Set up test for query failure
  - [ ] Create test: `it('should return null and log error when query fails', () => { ... })`
  - [ ] Arrange: 
    - [ ] Drop `tasks` table or cause query to fail
    - [ ] Mock logger to capture error logs
  - [ ] Act: Call `getNextReadyTask()`
  - [ ] Assert: 
    - [ ] Returns `null`
    - [ ] Error is logged

### Specific Requirements

- [ ] Requirement 1: Test must verify error handling
  - Acceptance: Query error returns `null` and logs error
- [ ] Requirement 2: Test must verify fail-safe behavior
  - Acceptance: Method returns null on error (doesn't throw)
- [ ] Requirement 3: Test must verify error logging
  - Acceptance: Logger receives error message

### Error Handling and Edge Cases

- [ ] Handle case where table doesn't exist (drop table)
- [ ] Handle case where database connection fails
- [ ] Verify error messages are descriptive
- [ ] Test with various error scenarios

### Testing

- [ ] Run `npm run test:unit` to verify test executes
- [ ] Verify test passes consistently
- [ ] Verify error logging works
- [ ] Run full test suite to ensure no regressions
- [ ] **DO NOT manually test by running the server** - use automated tests only

### Documentation

- [ ] Add comments explaining error handling
- [ ] Document why null is returned on error
- [ ] Note error logging approach

### Verification

- [ ] Verify test passes consistently
- [ ] Verify error handling works
- [ ] Verify error logging works
- [ ] Confirm test follows project conventions

## Notes

- This task is part of **Phase 1 – DatabaseService Unit Tests**
- **Execution Timing**: Should be executed after TASK-010.3 (completes TASK-010)
- **Dependencies**: 
  - TASK-004: Create a new test file for DatabaseService
  - TASK-010.3: Test getNextReadyTask ordering logic
- **Important Considerations**: 
  - Use in-memory database for isolation
  - Error handling returns null (fail-safe behavior)
  - Error logging is important for debugging
  - This is the fourth and final subtask for TASK-010
- **Task Independence**: Can be completed independently after TASK-010.3
- **Current State**: Test file exists with previous tests

## Related Tasks

- Parent: TASK-010 (Test getNextReadyTask behavior)
- Previous: TASK-010.3 (Test getNextReadyTask ordering logic)
- Next: TASK-011 (Test updateTaskStatus with updatedat column missing)
- Dependencies:
  - TASK-010.3: Test getNextReadyTask ordering logic
- Related:
  - TASK-010.1, TASK-010.2, TASK-010.3: Other subtasks for getNextReadyTask

## Definition of Done

### Task Type: TESTING TASK

**Description**: This task involves writing a test case for `getNextReadyTask` when query fails.

**Definition of Done**: "A test case is written that verifies `getNextReadyTask` returns `null` and logs an error when the database query fails. The test passes consistently. All changes have been committed to git and pushed to origin."

### User Stories

1. **As a developer**, I want to verify error handling works so that the system fails safely.
   - **Acceptance Criteria**: 
     - Test verifies query errors return `null` (fail-safe)
     - Test verifies errors are logged for debugging
     - Test passes consistently

### Automated Tests

The test case should be:

```typescript
describe('getNextReadyTask', () => {
  it('should return null and log error when query fails', () => {
    // Arrange: Drop table or cause error
    const db = createTestDatabase();
    // Don't create table, or drop it to cause error
    const mockLogger = { error: jest.fn() };
    const service = new DatabaseService(':memory:');
    // Inject mock logger or spy on logger
    
    // Act
    const result = service.getNextReadyTask();
    
    // Assert
    expect(result).toBeNull();
    expect(mockLogger.error).toHaveBeenCalled();
  });
});
```

- [ ] Test case written and passes
- [ ] Test verifies error returns null
- [ ] Test verifies error is logged

### Git Commit and Push Requirements

- [ ] Test case added to `tests/services/database-service.test.ts`
- [ ] Commit message follows project conventions (e.g., "test: add getNextReadyTask query failure test")
- [ ] Changes are pushed to origin
- [ ] Commit includes only this test case

### Final Checklist

- [ ] Test case written and passes
- [ ] Test verifies error handling
- [ ] Test verifies error logging
- [ ] All changes committed to git
- [ ] Code pushed to origin
- [ ] Task marked as complete
- [ ] **TASK-010 (all subtasks) is complete**

