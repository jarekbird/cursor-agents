# TASK-037.5: Test task-operator internal jobs - addDelayedAgent returns null

**Section**: 4. PromptProcessor Enhancements
**Subsection**: 4.3.5
**Task ID**: TASK-037.5

## Description

Add a test case for task-operator internal jobs to verify that when `addDelayedAgent` returns `null`, a skip message is logged. This handles the case where the task-operator already has existing jobs and doesn't need another one enqueued.

**Reference Implementation**: 
- `virtual-assistant/cursor-agents/src/queue/prompt-processor.ts` - task-operator internal job handling

## Current State

The `PromptProcessor.process()` method:
- If `addDelayedAgent` returns null, logs skip

The test file `tests/queue/prompt-processor.test.ts` exists with previous tests including TASK-037.1 through TASK-037.4.

**Important**: This task should be executed after TASK-037.4, completing TASK-037.

## Checklist

### Preparation and Setup

- [ ] Review `PromptProcessor.process()` addDelayedAgent null handling
- [ ] Understand when addDelayedAgent returns null
- [ ] Review logging patterns
- [ ] Review test setup from previous subtasks

### Implementation Steps

- [ ] Step 1: Write test for addDelayedAgent returns null
  - [ ] Create test: `it('should log skip when addDelayedAgent returns null', async () => { ... })`
  - [ ] Arrange: 
    - [ ] `addDelayedAgent` returns `null`
  - [ ] Act: Call `promptProcessor.process(job)`
  - [ ] Assert: 
    - [ ] Skip message logged

### Specific Requirements

- [ ] Requirement 1: Test must verify addDelayedAgent null case
  - Acceptance: Skip message logged
- [ ] Requirement 2: Test must verify logging
  - Acceptance: Logger receives skip message

### Error Handling and Edge Cases

- [ ] Verify skip message is descriptive
- [ ] Test with various scenarios that cause addDelayedAgent to return null

### Testing

- [ ] Run `npm run test:unit` to verify test executes
- [ ] Verify test passes consistently
- [ ] Verify existing tests still pass
- [ ] Run full test suite to ensure no regressions
- [ ] **DO NOT manually test by running the server** - use automated tests only

### Documentation

- [ ] Add comments explaining skip behavior
- [ ] Document why addDelayedAgent returns null

### Verification

- [ ] Verify test passes consistently
- [ ] Verify skip logging works
- [ ] Confirm test follows project conventions

## Notes

- This task is part of **Phase 4 – PromptProcessor Enhancements**
- **Execution Timing**: Should be executed after TASK-037.4 (completes TASK-037 and Phase 4)
- **Dependencies**: 
  - TASK-037.4: Test task-operator internal jobs - error case
- **Important Considerations**: 
  - Skip logging when addDelayedAgent returns null (e.g., task-operator has existing jobs)
  - This is the fifth and final subtask for TASK-037
- **Task Independence**: Can be completed independently after TASK-037.4
- **Current State**: Test file exists with previous tests

## Related Tasks

- Parent: TASK-037 (Add tests for task-operator internal jobs)
- Previous: TASK-037.4 (Test task-operator internal jobs - error case)
- Next: TASK-038 (Extend app.test.ts for Bull Board base path) - Starts Phase 5
- Dependencies:
  - TASK-037.4: Test task-operator internal jobs - error case
- Related:
  - TASK-037.1 through TASK-037.4: Other subtasks

## Definition of Done

### Task Type: TESTING TASK

**Description**: This task involves writing a test case for task-operator internal jobs when addDelayedAgent returns null.

**Definition of Done**: "A test case is written that verifies task-operator internal jobs log a skip message when addDelayedAgent returns null. The test passes consistently. All changes have been committed to git and pushed to origin."

### User Stories

1. **As a developer**, I want to verify skip behavior works so that unnecessary re-enqueueing is avoided.
   - **Acceptance Criteria**: 
     - Test verifies skip message is logged
     - Test passes consistently

### Automated Tests

The test case should be:

```typescript
describe('task-operator internal jobs', () => {
  it('should log skip when addDelayedAgent returns null', async () => {
    // Arrange: addDelayedAgent returns null
    // Act: promptProcessor.process(job)
    // Assert: Skip message logged
  });
});
```

- [ ] Test case written and passes
- [ ] Test verifies skip logging

### Git Commit and Push Requirements

- [ ] Test case added to `tests/queue/prompt-processor.test.ts`
- [ ] Commit message follows project conventions (e.g., "test: add task-operator addDelayedAgent null case test")
- [ ] Changes are pushed to origin
- [ ] Commit includes only this test case

### Final Checklist

- [ ] Test case written and passes
- [ ] Test verifies skip logging
- [ ] All changes committed to git
- [ ] Code pushed to origin
- [ ] Task marked as complete
- [ ] **TASK-037 (all subtasks) is complete**
- [ ] **Phase 4 (PromptProcessor Enhancements) is complete**

